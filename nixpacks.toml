[phases.setup]
# psycopg2-binary is a pre-compiled wheel — no PostgreSQL dev headers needed.
# Explicitly listing packages here prevents Nixpacks from auto-adding postgresql_16.dev.
nixPkgs = ["python312"]
